#include "main.h"
/**
 * _abs - Computes the absolute values of the integer
 *
 * Description: absolute numbers
 *
 *@r: detects number
 *
 * return: 1
 */

int _abs(int r)
{

	if (r >= 0)
		return (r);
	else
		return (r* -1);
}

